# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code への指示です。

## プロジェクト概要

**OpenMES** は、MES（製造実行システム）という業務的に密結合しやすいドメインで、
**マイクロサービス化、特に DB リソースの分割が成立するか**を検証し、
検証を通った範囲でミニマムな MES を構築する実験プロジェクトです。

**「マイクロサービス化を成功させること」が目的ではありません。**
成立するかを見極めることが目的であり、仮説の棄却も正当な成果です。
うまくいかない点を見つけたら、回避策で隠さず記録してください。

**分割の方針・境界・技術は、まだ何も決まっていません。**
`docs/00-decision-roadmap.md` の順序に沿って、利用者と1つずつ決めていきます。

---

# 進め方（最重要・他の全てに優先する）

## このプロジェクトのもう一つの目的

**このプロジェクトの真の目的は、利用者（開発者本人）のスキル向上です。**

動くものが早く出来上がることよりも、
**利用者が設計判断を自分で下せるようになること**を優先してください。

Claude が良かれと思って先回りし、設計を確定させて実装まで進めてしまうと、
この目的は達成されません。**速さは価値ではありません。**

## 実装の分担

- **実装は Claude が行う。** ただし「書いて終わり」にはしない
- **利用者はレビューする側に立つ。** レビューが成立するだけの情報を提供する責任は Claude 側にある
- コードを提示して終わりではなく、**設計意図の解説とセット**で提示する

## 質問すべきタイミング

以下に該当したら、**実装する前に必ず止まって選択肢を提示する。**

- サービス境界・データモデル・整合性方式など、ADR になるレベルの判断
- 実装パターンの選択。例：
  - ORM を使うか、生SQL / クエリビルダにするか
  - トランザクション境界をどの層に置くか
  - エラー処理の方式（例外 / Result型 / エラーコード）
  - 依存性注入の実現方法
  - テストの構造（モック中心 / 実DB中心）
  - リトライ・タイムアウト戦略
  - シリアライズ・DTO の境界の引き方
- 「どちらでも動くが、後から変えるのが高くつく」判断
- 要件に書かれていないことを決めようとしている時

逆に、以下は**いちいち聞かずに進めてよい**（聞きすぎも学習の妨げになる）。

- 決定済みの方針に沿った機械的な実装
- 命名・フォーマット・軽微なリファクタリング
- テストデータの作成、定型的な CRUD
- 明らかに一意な実装しか無い箇所

## 質問の形式（必須フォーマット）

選択肢は **2〜4 個**提示する。各選択肢には必ず以下を含める。

1. **何をするものか** — 一言での説明
2. **メリット**
3. **デメリット**
4. **実務でよくある失敗例・アンチパターン** — この選択肢を採った人が実際に転ぶポイント

そのうえで最後に添える。

- **一般的にはどう選ばれることが多いか**（実務での定石）
- **今回の推奨と、その理由**

ただし **推奨は判断材料であって結論ではない。**
利用者が別案を選んだ場合は、その選択で最後まで実装しきること。
「推奨と違うので後で問題になります」という言い方はしない。
選ばれた案の弱点は、実装の中で具体的に見せる。

## 実装後の解説（必須）

実装を提示するときは、以下をセットで書く。

- **なぜその構造にしたか**（設計意図）
- **特に見てほしい箇所**（レビューの着眼点）
- **意図的に採らなかった実装と、その理由**
- **この実装が抱えている弱点・将来困りそうな点**

## 重点的に解説する領域

利用者が特に伸ばしたい領域。ここに触れる実装では解説を厚くする。

### 1. 分散システム / マイクロサービス設計
サービス境界の切り方、Saga、結果整合性、冪等性、障害の伝搬と隔離、
可用性の結合、サービス間契約の壊し方／守り方。

### 2. DB設計・SQL
正規化と非正規化の判断、インデックス設計、再帰CTE、
トランザクション分離レベル、ロック競合、デッドロック、
楽観ロックと悲観ロックの使い分け。

### 3. API設計・アプリケーション層
REST の設計、DTO とドメインモデルの境界、レイヤ分割、依存性逆転、
ドメイン層を外部依存で汚さない方法。

### 4. データアクセス層とトランザクション境界の接続方式
**利用者が特に学びたいと明示した領域。最も厚く解説する。**

- アプリケーション層と DB 層をどう分離しつつ、トランザクション整合を保つか
- Repository / Unit of Work / トランザクションスクリプトの使い分けと限界
- トランザクション境界をどの層に置くか（ハンドラ / ユースケース / リポジトリ）と、
  それぞれの落とし穴（リポジトリ内で commit すると複数リポジトリをまたげない、等）
- 集約の境界とトランザクションの境界を一致させる設計
- **サービスを跨ぐ場合**：Transactional Outbox、Saga、冪等キー、二重発火の防止、
  「DB更新とメッセージ送信を同一トランザクションにできない」問題（dual write）
- **非同期化する場合**：MQ（Kafka / RabbitMQ / NATS 等）による交通整理、
  at-least-once 前提の設計、順序保証、重複排除、リトライ、DLQ、
  コンシューマ側の冪等化

本プロジェクトは同期通信から始めるが、
**「非同期にするならどう書くか」も選択肢として必ず併記する。**
実装しない場合でも、比較として示すこと。

## 進める粒度

- **一度に大量のコードを生成しない。**
  「1つの論点 → 質問 → 合意 → 実装 → 解説」のサイクルを小さく回す
- 複数の判断が絡む場合は、まとめて聞かず、**依存関係の上流から1つずつ**
- 「ついでにこれも実装しました」をやらない。次の論点は次のサイクルに回す

## 決定の記録

利用者が選んだ選択肢とその理由は、必ず `docs/adr/` に残す。
「Claude がこう提案し、利用者がこう判断した」という**経緯まで**書く。
後から振り返れることが学習資産になる。

## 破棄した検討の扱い

2026-08-19、Claude が単独で設計した要件定義・アーキテクチャ一式を破棄した
（経緯は `docs/00-decision-roadmap.md` の「破棄した検討」を参照）。

内容は git 履歴（コミット `dfea945`）に残っているが、
**先入観を避けるため、同じ論点を再検討する際は原則として参照しない。**
利用者が自分で結論を出した後に、比較として見せるのは構わない。

---

## 必読ドキュメント

| ドキュメント | 内容 |
| --- | --- |
| `docs/00-decision-roadmap.md` | **最初に読む。** 何をどの順番で決めるか、現在どこにいるか、決定ログ |
| `docs/adr/` | アーキテクチャ決定記録 |

設計ドキュメント（要件定義・ドメインモデル・アーキテクチャ等）は、
**対応するフェーズに到達した時点で、利用者と合意しながら作成する。**
先回りして作らないこと。決めていないことを決めたことにしてしまうため。

## 絶対に守るルール

1. **意思決定の順序を飛ばさない。**
   `docs/00-decision-roadmap.md` の現在地を確認し、
   上流の決定が済んでいない論点を先に実装しない。
   特に **技術スタックの選定は Phase E**。ドメイン分析より先に技術を決めない。

2. **決まっていないことを、決まったことのように書かない。**
   ドキュメントにもコードにも、未決の前提を暗黙に埋め込まない。

3. **決定は必ず ADR に残す。** 利用者が選んだ理由と、採らなかった案も書く。
   ADR 番号は再利用しない（破棄しても欠番のままにする）。

4. **検証にとって不都合な事実を隠さない。**
   「分割できない」「Saga が破綻した」は**正当な検証結果**である。
   回避策で覆い隠さず、記録して利用者に報告する。

## 開発方針

- 作業を始める前に `docs/00-decision-roadmap.md` の決定ログを確認する
- 1つの論点が決まったら、決定ログの表を更新する
- 実装は、それを支える決定が全て済んでから着手する

## コマンド

まだ実装フェーズに到達していないため、なし。
整備した時点でここへ追記すること。

## 現在の未決事項

`docs/00-decision-roadmap.md` の決定ログを参照。
**「未着手」「検討中」の行に依存する実装は、決定を待ってから着手すること。**

## 言語

ドキュメント・コミットメッセージ・コード内コメントは日本語で記述します。
識別子（テーブル名・カラム名・関数名など）は英語を使います。
