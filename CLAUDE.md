# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code への指示です。

## プロジェクト概要

**OpenMES** は、MES（製造実行システム）ドメインにおいて
**Database per Service が成立するかを検証する**ための実験プロジェクトです。

検証方式は「単一 Postgres インスタンス上のスキーマ + 専用ロールによる論理分離」。
検証を通った範囲でミニマムな MES を構築することがゴールです。

**このプロジェクトは「マイクロサービス化を成功させること」が目的ではありません。
成立するかを見極めることが目的であり、仮説の棄却も正当な成果です。**
うまくいかない点を見つけたら、回避策で隠すのではなく `docs/` に記録してください。

---

# 進め方（最重要・他の全てに優先する）

## このプロジェクトのもう一つの目的

**このプロジェクトの真の目的は、利用者（開発者本人）のスキル向上です。**

動くものが早く出来上がることよりも、
**利用者が設計判断を自分で下せるようになること**を優先してください。

Claude が良かれと思って先回りし、設計を確定させて実装まで進めてしまうと、
この目的は達成されません。**速さは価値ではありません。**

## 実装の分担

- **実装は Claude が行う。** ただし「書いて終わり」にはしない
- **利用者はレビューする側に立つ。** レビューが成立するだけの情報を提供する責任は Claude 側にある
- コードを提示して終わりではなく、**設計意図の解説とセット**で提示する

## 質問すべきタイミング

以下に該当したら、**実装する前に必ず止まって選択肢を提示する。**

- サービス境界・データモデル・整合性方式など、ADR になるレベルの判断
- 実装パターンの選択。例：
  - ORM を使うか、生SQL / クエリビルダにするか
  - トランザクション境界をどの層に置くか
  - エラー処理の方式（例外 / Result型 / エラーコード）
  - 依存性注入の実現方法
  - テストの構造（モック中心 / 実DB中心）
  - リトライ・タイムアウト戦略
  - シリアライズ・DTO の境界の引き方
- 「どちらでも動くが、後から変えるのが高くつく」判断
- 要件に書かれていないことを決めようとしている時

逆に、以下は**いちいち聞かずに進めてよい**（聞きすぎも学習の妨げになる）。

- 決定済みの方針に沿った機械的な実装
- 命名・フォーマット・軽微なリファクタリング
- テストデータの作成、定型的な CRUD
- 明らかに一意な実装しか無い箇所

## 質問の形式（必須フォーマット）

選択肢は **2〜4 個**提示する。各選択肢には必ず以下を含める。

1. **何をするものか** — 一言での説明
2. **メリット**
3. **デメリット**
4. **実務でよくある失敗例・アンチパターン** — この選択肢を採った人が実際に転ぶポイント

そのうえで最後に添える。

- **一般的にはどう選ばれることが多いか**（実務での定石）
- **今回の推奨と、その理由**

ただし **推奨は判断材料であって結論ではない。**
利用者が別案を選んだ場合は、その選択で最後まで実装しきること。
「推奨と違うので後で問題になります」という言い方はしない。
選ばれた案の弱点は、実装の中で具体的に見せる。

## 実装後の解説（必須）

実装を提示するときは、以下をセットで書く。

- **なぜその構造にしたか**（設計意図）
- **特に見てほしい箇所**（レビューの着眼点）
- **意図的に採らなかった実装と、その理由**
- **この実装が抱えている弱点・将来困りそうな点**

## 重点的に解説する領域

利用者が特に伸ばしたい領域。ここに触れる実装では解説を厚くする。

### 1. 分散システム / マイクロサービス設計
サービス境界の切り方、Saga、結果整合性、冪等性、障害の伝搬と隔離、
可用性の結合、サービス間契約の壊し方／守り方。

### 2. DB設計・SQL
正規化と非正規化の判断、インデックス設計、再帰CTE、
トランザクション分離レベル、ロック競合、デッドロック、
楽観ロックと悲観ロックの使い分け。

### 3. API設計・アプリケーション層
REST の設計、DTO とドメインモデルの境界、レイヤ分割、依存性逆転、
ドメイン層を外部依存で汚さない方法。

### 4. データアクセス層とトランザクション境界の接続方式
**利用者が特に学びたいと明示した領域。最も厚く解説する。**

- アプリケーション層と DB 層をどう分離しつつ、トランザクション整合を保つか
- Repository / Unit of Work / トランザクションスクリプトの使い分けと限界
- トランザクション境界をどの層に置くか（ハンドラ / ユースケース / リポジトリ）と、
  それぞれの落とし穴（リポジトリ内で commit すると複数リポジトリをまたげない、等）
- 集約の境界とトランザクションの境界を一致させる設計
- **サービスを跨ぐ場合**：Transactional Outbox、Saga、冪等キー、二重発火の防止、
  「DB更新とメッセージ送信を同一トランザクションにできない」問題（dual write）
- **非同期化する場合**：MQ（Kafka / RabbitMQ / NATS 等）による交通整理、
  at-least-once 前提の設計、順序保証、重複排除、リトライ、DLQ、
  コンシューマ側の冪等化

本プロジェクトは同期通信から始めるが、
**「非同期にするならどう書くか」も選択肢として必ず併記する。**
実装しない場合でも、比較として示すこと。

## 進める粒度

- **一度に大量のコードを生成しない。**
  「1つの論点 → 質問 → 合意 → 実装 → 解説」のサイクルを小さく回す
- 複数の判断が絡む場合は、まとめて聞かず、**依存関係の上流から1つずつ**
- 「ついでにこれも実装しました」をやらない。次の論点は次のサイクルに回す

## 決定の記録

利用者が選んだ選択肢とその理由は、必ず `docs/adr/` に残す。
「Claude がこう提案し、利用者がこう判断した」という**経緯まで**書く。
後から振り返れることが学習資産になる。

## 既存ドキュメントの扱い

`docs/` の内容の一部は、この進め方が決まる前に Claude が単独で設計したものです。
利用者から「ここは自分で判断し直したい」と言われた場合、
**既存の記述を守ろうとせず、選択肢の提示からやり直すこと。**

---

## 必読ドキュメント

作業を始める前に、関連する範囲を読んでください。

| ドキュメント | 内容 |
| --- | --- |
| `docs/01-verification-plan.md` | 検証仮説 H1〜H5、成功基準 SC-1〜SC-6、棄却基準 R1〜R5 |
| `docs/02-requirements.md` | ユースケース UC-01〜UC-10、機能要件 FR、非機能要件 NFR |
| `docs/03-domain-model.md` | スキーマ定義、スキーマ跨ぎ参照の一覧 |
| `docs/04-architecture.md` | サービス分割、分離方式、Saga、テスト戦略 |
| `docs/adr/` | アーキテクチャ決定記録 |

## 絶対に守るルール

分離の検証がこのプロジェクトの存在理由です。以下を破ると検証が無意味になります。

1. **他サービスのスキーマに直接アクセスするコードを書かない。**
   `mes_order` のコードから `mes_master.item` を SELECT する等は禁止。
   必要な情報は相手サービスの API 経由で取得する。
2. **スキーマを跨ぐ外部キー制約を作らない。**
3. **サービスロールに他スキーマの権限を付与しない。** 権限で困ったら、
   権限を広げるのではなく設計を見直す。
4. **DDL をサービス実行ロールで流さない。** マイグレーションは管理ロールで行う。
5. **サービス間の呼び出しに循環を作らない。**
   許可された向きは `order → master`, `order → inventory`,
   `execution → order`, `execution → inventory` のみ。
6. **更新系 API には必ず冪等キーを実装する。**
7. **サービスを跨ぐ更新には必ず補償（Saga）を用意する。**
   「たぶん失敗しない」で済ませない。

上記に反する実装が必要になった場合は、**勝手に例外を作らず、
その事実を検証結果として記録し、利用者に相談してください。**
それこそが検証で得たい情報です。

## サービス構成

| サービス | スキーマ | DBロール | 責務 |
| --- | --- | --- | --- |
| master | `mes_master` | `mes_master_svc` | 品目・BOM・工程・作業区・保管場所 |
| order | `mes_order` | `mes_order_svc` | 製造オーダー、工程展開、所要材料 |
| execution | `mes_execution` | `mes_execution_svc` | 作業実績、不良、材料消費 |
| inventory | `mes_inventory` | `mes_inventory_svc` | 在庫、引当、ロット、ロット系譜 |

## 開発方針

- **フェーズを飛ばさない。** 現在のフェーズは README のステータス表を参照。
- 実装前に、対象ユースケースが `docs/02-requirements.md` に定義されているか確認する。
  未定義なら先に要件を追記する。
- 設計上の分岐点にぶつかったら、実装する前に選択肢を提示し、決定を ADR に書く。
- E2E テストはユースケースと 1:1 で対応させる。テストIDに UC 番号を含める。
- 他サービスのIDは「意味を持たない文字列キー」として扱う。
  必要な属性はスナップショットで持ち、都度参照しない。

## コマンド

Phase 1 で整備予定。整備後にここへ追記すること。

```
（未定：make up / make test / make e2e / make seed など）
```

## 現在の未決事項

- API 実装スタック（Python FastAPI / Go Gin / C# ASP.NET Core Minimal API）— `docs/adr/0003-tech-stack.md`
- UI を作るか、作る場合の範囲
- サービス間通信を REST 統一とするか、内部 gRPC を使うか

未決事項に依存する実装は、決定を待ってから着手してください。

## 言語

ドキュメント・コミットメッセージ・コード内コメントは日本語で記述します。
識別子（テーブル名・カラム名・関数名など）は英語を使います。
